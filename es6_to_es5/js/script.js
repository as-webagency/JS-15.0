window.addEventListener( 'DOMContentLoaded', () => {
	'use strict';

    // Timer
    countTimer( '20 November 2020' );

    // Menu
    toggleMenu();

    // Popup
    togglePopup();

    // Tabs
    toggleTabs();

    // Slider
    slider();

    // Team
    ourTeam();

    // Calculator
    calc( 100 );

    // Ajax Form
    sendFormsAjax();
});